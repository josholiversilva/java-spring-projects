type Mutation {
    # Drinks
    createDrink(
        name: String,
        sweetness: Int,
        rating: Float,
        restaurant_id: Int
    ) : Drink

    # Restaurants
    createRestaurant(
        name: String
        location: String
    ): Restaurant
}